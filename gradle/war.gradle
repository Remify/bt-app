apply plugin: "war"

bootWar {
    mainClassName = "fr.bt.backend.BtApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
